{
  "name": "news-aggregator-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "fetch": "node scripts/fetch_rss.js",
    "build": "eleventy",
    "prepare": "npm run fetch && npm run build"
  },
  "dependencies": {
    "rss-parser": "^3.12.0",
    "@11ty/eleventy": "^2.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
